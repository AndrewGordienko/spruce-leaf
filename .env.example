# spruce-leaf needs NO API key: it reasons through your local `claude` CLI
# (Claude Code), using your existing authentication. Just make sure `claude`
# is installed and on your PATH:  claude --version
#
# This file is optional and kept only for any other local settings you want
# auto-loaded at startup.
