# OutageHub — utility-outage data API for distributed operations.

key = "outagehub"
name = "OutageHub"
signature = "Andrew Gordienko"

one_liner = "OutageHub provides normalized live and historical Canadian utility-outage data that operating systems can query by location and time."

verified_seller_facts = [
  "OutageHub exposes time-range outage queries, individual outage details, and provider coverage through an API authenticated with an API key.",
  "OutageHub records geographic outage polygons and provider-reported fields such as affected-customer count, cause, outage type, planned status, start time, and estimated restoration time when those fields are available.",
  "OutageHub can compare a verified operated Canadian address with historical public utility outage areas and timestamps.",
  "A historical match is outside confirmation that a location overlapped a reported utility event; it is not proof of private charger or site status.",
]

motion = "find one location-specific diagnosis, dispatch, escalation, continuity, prioritization, or communication decision that can improve with live or historical Canadian utility-outage data"

icp_note = "Cover every credible Canadian API customer with distributed outage-sensitive sites or assets: EV charging, telecom, cold storage, data centres, multi-site retail and facilities, backup-power/service dispatch, critical infrastructure, and adjacent operators. Work easy to hard: visible decision + historical match + reachable owner first; visible decision without a match next; generic footprint last."

min_words = 60
max_words = 95

min_signals = 3

system_concept_examples = [
  "an API result showing that an operating location overlapped a utility-reported outage at a specific time",
  "an API lookup that returns utility reports for a supplied Canadian address and time",
  "a location-matched utility webhook for a customer-confirmed operating system",
]

subject_examples = [
  "Separating utility and site incidents",
  "Power checks for lab sites",
  "Store power issue checks",
  "Tower dispatch after power loss",
]

vantage_notes = [
  "Service Operations / network operations owner: can explain the account's site-diagnosis workflow and whether outside utility context would change it.",
  "Operations, facilities, dispatch, continuity, or reliability leader: can answer the account-specific decision question or route it to the daily workflow owner.",
  "Technical evaluator: relevant only after the manual check and its consequence are confirmed.",
  "Commercial, customer-success, or general corporate contacts are route-only unless evidence proves they own site diagnosis.",
]

requirements = [
  "For a one-email discovery test, research must establish a distributed Canadian operating footprint, one specific outage-time decision, and a reachable workflow owner. A completed historical match upgrades the account to easy priority but is not required for the first question.",
  "A two-email evidence sequence requires a completed historical location-to-utility-event match; T2 cannot present a match without the real location and timestamp.",
  "T1 contains one account-specific diagnosis, dispatch, escalation, continuity, prioritization, or communication moment and a plausible consequence. Do not prescribe a universal dark-site, ticket, dispatch, or internal-system binary.",
  "T1 names OutageHub and explains its location-matched Canadian utility signal in one plain clause. Discovery-ready copy asks for one easy operating answer by email; only action-ready copy may offer a short conversation with an email alternative.",
  "When present, a later touch may contribute the completed historical result: the verified location, utility-reported event, and actual timestamp. Without a match, later touches must add another source-supported decision-useful angle rather than implying one exists.",
  "The default is one discovery email. A verified historical match and correct decision owner may earn a two- or four-touch sequence. Seven-touch OutageHub sequences are retired.",
  "Public utility data is outside confirmation, not proof of charger status and not a replacement for telemetry. State that boundary naturally at most once across the two emails.",
  "Write like an operator discussing site diagnosis. Do not expose phrases such as account file, research, source material, external context, reported interruption, or operational action.",
]

forbidden = [
  "operational resilience",
  "proactive decision-making",
  "proactive decision making",
  "enhance operational",
  "not selling",
  "enable proactive",
  "first-trip classify",
  "classification path",
  "dispatch-versus-wait",
  "when a power-related disruption appears",
  "external context alongside monitoring",
  "which response step takes the most time",
  "does that distinction recur",
  "reported interruption",
  "next operational action",
  "location-matched external context",
  "account file reports",
  "account file",
  "manually reconcile",
  "manually reconciling",
  "close this out",
  "closing the loop",
]

copy_doctrine = """
Cover the market broadly, but make every email narrow. Keep evidence readiness
separate from cash-now, core, and strategic commercial lanes. Action-ready
opportunities expose a specific outage-time decision, a reachable owner, and a
completed location/polygon match. Discovery-ready opportunities expose the
decision and owner but lack a match; they may receive one complete first email
only. Research-required opportunities remain in research.

T1 is 60–95 words and contains one account-specific operating question about
diagnosis, dispatch, escalation, continuity, prioritization, or communication.
Explain why this person was chosen, state the consequence conservatively, and
name OutageHub's location-matched Canadian utility API. Ask for one easy answer
by email while the account is discovery-ready; reserve a conversation option
for action-ready copy. Never reuse a universal
site-goes-dark or equipment-ticket fork.

When an action-ready account receives a two- or four-touch review sequence, every later step
must earn its existence with a distinct supported angle. A verified location,
utility-reported event, and timestamp can anchor one touch. Other touches may
cover the outside-context boundary, provider completeness, a bounded workflow
example, or an evaluation path. No real match means no historical claim.
"""

doctrine = """
The long-term market is every organization whose operating software or people
make a Canadian location-specific decision that outside utility data can
improve. EV charging is one wedge, alongside telecom, facilities, cold storage,
data centres, service dispatch, backup power, multi-site operations, and other
outage-sensitive infrastructure.

Research does the hard work before copy:
  1. Verify a distributed Canadian footprint.
  2. Identify one exact outage-time decision and its nearest owner.
  3. Separate private telemetry from public utility context.
  4. When possible, preserve a genuine historical location/polygon overlap,
     timestamp, source, and what the API would have returned.

T1 makes the account-specific hypothesis and OutageHub contribution clear
enough to justify a short conversation or email answer. Existing telemetry may
already settle the question; ask precisely rather than asserting a gap. A real
historical result can support T2. Never invent more touches to rescue a generic
hypothesis.
"""
